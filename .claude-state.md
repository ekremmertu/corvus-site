# corvus-site — Proje State

## Aktif Durum
- **Proje:** Corvus Tech portfolyo/pazarlama sitesi (piyasaya çıkış)
- **Faz:** F3 — **CANLI YAYINDA.** `https://corvus-tech.co` Vercel'den servis ediliyor. **Marka sürümü + ürün linkleri canlıya alındı 2026-08-23**, son commit `2744c62` (deploy `corvus-site-dcjy4y1fq`), **28 proje / 67 sayfa**
- **Tarih:** 2026-08-23
- **Domain:** `corvus-tech.co` (TİRELİ — GoDaddy, NS ns05/ns06.domaincontrol.com). Kök = portfolyo, `/terminal` = Corvus Tech Terminal.
- **⚠️ Ders:** İlk seansta yanlışlıkla tiresiz `corvustech.co` (sahibi başkası, Atak Domain) kullanıldı → 2026-07-30'da düzeltildi (`96271fc`), Vercel'den kaldırıldı.
- **DNS ✅ TAMAM:** CEO GoDaddy'de A `@`→`76.76.21.21`, CNAME `www`→`cname.vercel-dns.com` yaptı. Otoriter NS + 8.8.8.8 doğrulandı.
- **SSL ✅ TAMAM:** `cert_K8vvQUGWFbX4nXGdeWRan4eI` (Let's Encrypt, corvus-tech.co + www, 2026-10-28'e kadar, auto-renew açık)
- **🔒 GÜVENLİK TURU (2026-07-30):** `/terminal` YAYINDAN KALDIRILDI (auth bypass'lı admin paneli internete açıktı). Firestore public read kapatıldı, Ortak API'ye anahtar koruması eklendi, portfolyoya güvenlik başlıkları eklendi. Detay: TODO.md PASS LOG.
- **Stack:** Next.js 16.2.12 (Turbopack) + React 19 + Tailwind v4 + three@0.185 / @react-three/fiber@9 / drei@10
- **Build:** ✅ `npm run build` temiz — 0 TS hatası, 67 sayfa (2 locale × (home + work + 29 proje) + sitemap/robots/OG)
- **Git:** repo var (create-next-app init), henüz ilk anlamlı commit atılmadı → bu oturumda atılıyor
- **Test:** test altyapısı yok (statik pazarlama sitesi; görsel doğrulama Playwright MCP ile yapıldı — hero/stage/work/faq/contact/proje/mobil TR ekran görüntüleri PASS)

## Ne Kuruldu (mimari harita)
- `src/data/projects.ts` — **tek içerik kaynağı**: 5 kategori (ios/web/fintech/ai/enterprise) + 29 proje, tüm metinler EN+TR
- `src/i18n/dict.ts` — UI sözlüğü (nav/hero/process/faq/contact/footer), `Locale = en|tr`
- `proxy.ts` — Next 16 proxy konvansiyonu; Accept-Language → `/en` | `/tr` redirect
- `src/app/[lang]/layout.tsx` — fontlar (Archivo/Inter/JetBrains Mono), metadata + hreflang + JSON-LD, SceneProvider + SceneLayer + Nav + Footer
- `src/app/[lang]/page.tsx` — Hero → Marquee → Stats → DisciplineStage → HomeWork → Process → Faq → Contact
- `src/app/[lang]/work/page.tsx` — filtreli grid (`?d=<slug>` başlangıç filtresi)
- `src/app/[lang]/work/[slug]/page.tsx` — proje detay (SSG 58 sayfa) + next-project nav
- `src/components/scene/` — SceneProvider (state+ref'ler), SceneCanvas (tek fixed Canvas), objects.tsx (5 disiplin objesi), SceneLayer (fallback'li)
- SEO: `sitemap.ts`, `robots.ts`, `[lang]/opengraph-image.tsx` (ImageResponse), `not-found.tsx`
- `DESIGN_LOG.md` — Web App Designer Agent PROJECT LOG v2 (tokens, kararlar, elenenler, hipotezler)

## Kritik Kararlar
- **Ciao Energy tarzı tek sürekli 3D sahne** (CEO seçimi; video kare kare analiz edildi): carousel hero → scroll-pinned disiplin sahnesi (obje döner, 4 içerik durağı) → exit
- **`--c-live` sistemi:** aktif disiplin rengi CSS custom property ile tüm DOM'u boyar; SceneProvider yazar
- **⚠️ NESTLÉ ADI ASLA GEÇMEZ** — kurumsal işler "Global bir FMCG şirketi" (CEO: "çok önemli"). `projects.ts` içindeki 3 enterprise projede `client` alanı böyle.
- Progressive enhancement: WebGL yok / reduced-motion / <768px / ≤4 çekirdek → 3D kapalı, CSS halo fallback; içerik mobilde düz liste
- `stageRef/stopRef/exitRef` render tetiklemez, rAF içinde okunur (perf)
- `body { background: transparent }` + arka plan `html`'de — fixed `-z-10` canvas'ın görünmesi için (bilinen tuzak, çözüldü)

## Bilinen Issue / Placeholder
| # | Konu | Durum |
|---|------|-------|
| 1 | ~~`src/lib/site.ts` e-posta placeholder~~ | ✅ ÇÖZÜLDÜ — `uekremmert@gmail.com` teyit edildi, `28477be`. ⚠️ Kurumsal görünüm için `hello@corvus-tech.co` kutusu hâlâ kurulmadı |
| 2 | Proje kartlarında gerçek ekran görüntüsü yok (bilinçli v1.1) | Asset'ler mevcut: Quill/marketing, TripWalkers/fastlane/screenshots, Amelie.co/Logo |
| 3 | `/[lang]/work` dynamic render (searchParams) — sorun değil, Vercel'de edge | — |
| 4 | THREE.Clock deprecation uyarısı (drei içinden) — zararsız | — |

## ⚠️ /terminal KALDIRILDI (2026-07-30 güvenlik turu)
- **Neden:** `contexts/AuthContext.tsx` içinde giriş kontrolü bypass edilmişti (`useState` varsayılanı admin dolu, "Login devre dışı — auto-admin" yorumu). Yani `corvus-tech.co/terminal` adresini bilen HERKES admin dashboard'unu görüyordu. Admin şifre özeti de (tuzsuz SHA-256) canlı bundle'da açıktaydı.
- **Karar (CEO):** Terminal iç kullanım aracı — auth eklemek yerine yayından tamamen kaldırıldı. Saldırı yüzeyini kilitlemek yerine yok etme tercihi. Local'de `npm run dev` ile kullanılıyor.
- **Portfolyo tarafı:** `next.config.ts` rewrites silindi · `src/proxy.ts` /terminal muafiyeti silindi · `Footer.tsx` Terminal linki silindi. (`TerminalEgg`/`TerminalTrigger` easter egg'leri site içi efekt, /terminal'e link vermiyor — dokunulmadı.)
- **Firebase tarafı:** `firebase hosting:disable --project corvus-tech` → `corvus-tech.web.app` + `/terminal/` artık 404. `firebase.json`'dan hosting bloğu çıkarıldı, `firebase.hosting.disabled.json`'a yedeklendi (geri açma talimatı içinde).
- **Doğrulama:** `corvus-tech.co/terminal` → 307 → `/en/terminal` → **404** ✓ · `corvus-tech.web.app/terminal/` → **404** ✓
- **GERİ AÇILACAKSA:** önce auth eklenmeli (Supabase Magic Link altyapısı hazır ama bağlı değil — `services/supabase.ts` ölü kod, `info.ts`'te "Auth: Magic Link" yazıyor).

## /terminal Entegrasyonu (2026-07-29, tamam — ARTIK GEÇERSİZ, yukarıya bak)
- **Terminal repo:** `~/Documents/GitHub/Corvus-Tech-Terminal` (commit `ac4772f`): `vite.config.ts` `base: '/terminal/'` + `firebase.json` public=`deploy` (dist → `deploy/terminal/` kopyalanır), rewrite `**`→`/terminal/index.html`, kök→`/terminal` 301
- **Firebase deploy edildi ✅:** `corvus-tech.web.app/terminal/` 200, asset'ler `/terminal/assets/...` 200
- **Portfolyo (`683e6de`):** `next.config.ts` rewrites `/terminal(/:path*)` → `corvus-tech.web.app/terminal/...`; `src/proxy.ts` /terminal'i locale redirect'ten muaf tutar (**dikkat: proxy.ts `src/` altında olmalı, kökte çalışmıyor** — bug bulundu, taşındı); footer'a Terminal linki
- Terminal'in `localhost:8765` CSP hataları **eskiden beri var** (bilinçli local-only mode, cloud billing kapalı) — dokunulmadı
- Terminal yeniden deploy akışı: `npm run build && rm -rf deploy && mkdir -p deploy/terminal && cp -R dist/ deploy/terminal/ && firebase deploy --only hosting --project corvus-tech`

## Vercel Deploy + DNS (2026-07-30, TAMAM — canlı)
- Login: `ekremmertu-1665` · Proje: `corvus-site` (`prj_vP8vdLQrBWQTs1GkSMxD58t1xc4e`, merts-projects-43159155)
- **Canlı:** https://corvus-tech.co · yedek URL https://corvus-site-omega.vercel.app
- Redeploy: `npx vercel --prod --yes` (corvus-site klasöründe)
- **⚠️ ÖĞRENİLEN DERS — SSL otomatik çıkmadı:** DNS doğru yayıldıktan sonra Vercel 15+ dk boyunca sertifika üretmedi (TLS handshake `SSL_ERROR_SYSCALL`, `vercel certs ls` boştu). Çözüm: **`vercel certs issue corvus-tech.co www.corvus-tech.co`** → 12 sn'de çıktı. Yeni domain bağlarken sertifika 10 dk'da gelmezse otomatik beklemeyi bırak, elle tetikle.
- **Teşhis notu:** `76.76.21.21` erişilebilirliğini test etmek için çalışan başka bir domaini o IP'ye zorla: `curl --resolve ameliea.co:443:76.76.21.21 https://ameliea.co` → 200 dönüyorsa ağ değil sertifika sorunudur.
- **Yerel DNS cache tuzağı:** macOS resolver eski IP'yi (199.36.158.100 / Firebase) tutmaya devam eder, `curl https://corvus-tech.co` yanıltıcı şekilde Firebase'i döner. Gerçeği görmek için `dig @ns05.domaincontrol.com` (otoriter) + `curl --resolve ...:76.76.21.21` kullan. Temizleme: `sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder`

## Marka Kimliği + Açılış (2026-07-30 yapıldı, commit `28477be` + canlıya alındı 2026-08-23)
- **Logo mavi oldu.** Kaynak lime/zeytin logo (`OneDrive/.../Post/2.post.png`) site paletine taşındı → `#5B8CFF`. Varyantlar `public/brand/` (logo / mark / wordmark / white / social-1x1). Kaynak dosyalar ve 4K/master medya `brand-src/` (public dışı — Vercel bundle'ına girmesin).
- **Nav işareti:** `.brand-mark` CSS mask (PNG alfa) + `background: var(--c-live)` → işaret aktif disiplin rengiyle değişmeye devam eder. Sabit renkli `<img>` KULLANMA, bu özellik kaybolur.
- **`fx/Intro.tsx`** — ~3.8 sn terminal açılışı (Coinbase reklam formatı). sessionStorage `cv_intro` + `<head>` inline script (`data-intro-seen`) → tekrar yüklemede sıçrama yok. reduced-motion'da hiç oynamaz.
- **`fx/corvusAscii.ts`** — 84×35 ASCII kuzgun, `corvus-mark.png` alfasından üretildi. ⚠️ CSS'te `letter-spacing` 0 ve `line-height` 1.09 olmalı; başka değer monospace hücre oranını bozup kuzgunu gerer.
- **`BrandFilm.tsx`** — Hero altında `id="film"`, IntersectionObserver ile görünürken oynar. Hero'nun sağı fixed 3D canvas'a ait, video oraya konamaz.
- **Higgsfield:** `nano_banana_pro` 4K görsel (logo referanslı) + `kling3_0` 10 sn pro. **`end_image` = kendi logomuz** → AI logoyu uydurmuyor, videonun son 1.5 sn'si birebir marka kilidi. Toplam ~22 kredi. Preset önerisi reddedildi (start/end kontrolünü elden alıyor).
- **⚠️ Sitedeki video ASCII sürümdür, Higgsfield'ın sinematik çıktısı değil.** İlk teslim kılavuz formatına uymuyordu (gradyan/glow vardı, ASCII yoktu); CEO uyardı, düzeltildi. Higgsfield çıktısı **atılmadı** — ASCII katmanının kaynağı oldu (`brand-src/corvus-intro-cinematic.mp4` olarak da duruyor).
- **`brand-src/build_intro.py`** tek komutla videoyu baştan üretir (`python3 build_intro.py`). Chrome/headless gerekmez, saf PIL+ffmpeg. Senaryoyu değiştirmek = scriptteki `SCRIPT` dizisini düzenlemek. Kritik ayarlar ve neden öyle oldukları TODO.md PASS LOG'da tablo halinde.
- **Bilinen çakışma:** logo mavisi `#5B8CFF` = `--c-ios`. Ayrım kararı CEO'da (TODO #9).

## App Store Görselleri + Intro (2026-08-23)
- Görseller `public/appstore/<slug>/1..5.jpg` (tripwalkers, quill, lingoria, cvtoapply), veride `screenshots: 5` (sayı, yol değil). Kaynak: iTunes `screenshotUrls`, URL sonundaki `/320x480bb.jpg` → `/640x0w.jpg`.
- Galeri **App Store butonunun hemen altında**, `.shots-row` yatay scroll-snap.
- **Intro `layout.tsx`'te DEĞİL, `[lang]/page.tsx`'te** — sadece ana sayfa yenilemesinde oynar.

## Kart Sırası + Çift Kategori (2026-08-23)
- Sıra `rankOf()` ile sunucuda: 0 Yayında · 1 iOS geliyor · 2 diğer · 3 Perdeli · 4 Gizli. Kartın görünen rozetine göre, veri detayına göre değil.
- Bir projeyi ikinci sekmede de göstermek için `alsoIn: ["ios"]` yaz. Kopya `dup: true` alır, "Tümü"de sayılmaz, detay sayfası ortaktır.
- "iOS geliyor" rozeti yalnız `category === "ios"` kartlarda çıkar.

## Perdeleme Sistemi (2026-08-23) — DİKKAT
- Proje kartı gizlemek için `projects.ts`'te o projeye **`veil: "soon"`** veya **`veil: "confidential"`** ekle. Tek yer burası, başka dosyaya dokunma.
- **`src/data/taxonomy.ts` = istemci tarafı** (tipler + kategoriler). **`src/data/projects.ts` = sunucu tarafı** (tam metinler). Bir `"use client"` bileşeni `@/data/projects`'ten import ederse TÜM gizli metin JS paketine sızar.
- Perdeli projenin detay sayfası hiç üretilmez (404), sitemap'te ve terminal egg'inde yoktur, slug'ı bile maskelidir.
- Şu an perdeli 11: 8 iOS (yayında değil) + Müşteri Bul + 2 kurumsal (NDA).

## App Store Connect API (2026-08-23) — mağaza sorusunu buradan çöz
- ⛔ **`Diyez` siteye EKLENMEYECEK** (CEO kararı 2026-08-23). ASC'de var, portfolyoda yeri yok.
- **Yayınlanmamış uygulamanın metnini de verir:** `/v1/appStoreVersions/{versionId}/appStoreVersionLocalizations` → description/promotionalText/keywords. Kart metni yazarken UYDURMA, buradan al.
- Kimlik: `~/.blitz/asc-credentials.json`. PyJWT ES256 → `api.appstoreconnect.apple.com`. **Ücretsiz.**
- `/v1/apps?limit=200` → 18 uygulama · `/v1/apps/{id}/appStoreVersions?limit=1` → `appStoreState`.
- **Gerçekten yayında 5:** TripWalkers, Quill, CVtoapply, Lingoria, BlokBom!.
- **PREPARE_FOR_SUBMISSION (hiç gönderilmemiş):** SplitTable, SplitTable Manager, Ameliea, Dermia, NöbetGO, Müşteri Bul, SocietyFood.
- ~~Corvus Budget~~ — proje İPTAL, 2026-08-23'te siteden tamamen silindi.

## App Store Geliştirici Hesabı (2026-08-23)
- **`Arzu UGUR`, artistId `1873744126`** — tüm iOS uygulamaları bu hesapta.
- Mağaza linkini **isimle arayarak bulma**; hesaptan listele: `curl -s "https://itunes.apple.com/lookup?id=1873744126&entity=software&country=tr"`. "Splittable" ve birçok "Quill" başkalarına ait.
- Yayında: TripWalkers `6764424121` · Quill `6768395513` · CVtoapply `6790497658` · Lingoria `6769572261` (sitede yok) · BlokBom! `6787959211` (sitede yok).
- ⚠️ Alan adı tuzakları: site `cvtoapply.co` (`.com` boş) · `splittable.me` (`.co` park sayfası) · `ameliea.co` (`amelie.co` başkasının).

## Marka İkonları (2026-08-23)
- `src/app/favicon.ico` + `icon.png` + `apple-icon.png` — hepsi `public/brand/corvus-mark.png`'den türetildi. Yeniden üretmek gerekirse: 1024'te `#05060a` zemine düzleştir → küçült → **RGBA olarak** kaydet (RGB ICO Turbopack'i patlatır).
- **Intro HER sayfa yüklemesinde oynar** (CEO kararı, `c4680c8`). Oturum kilidi yok. Tek istisna işletim sistemindeki "hareketi azalt"; `?intro=1` onu da geçersiz kılar.
- ⚠️ **Makine diski %100 doluydu (2026-08-23)** — `ENOSPC`/`ENOTEMPTY` hataları buradan geliyor. Ayrıntı ve temizlik listesi TODO.md 4. tur PASS LOG'unda.

## Sonraki İş
0. **SplitTable gerçek App Store linki** — uygulama yayınlanınca `projects.ts`'te `appStoreSoon: true` SİL, `appStoreUrl` ekle. ⚠️ Store'daki "Splittable: AI Bill Split Scan" (George King) BAŞKASININ, onu koyma.
0b. **splittable.co'ya gerçek site** — şu an GoDaddy park sayfası, site linki canlıda ona gidiyor (CEO bilerek onayladı).
1. **LinkedIn linkini siteye ekle** — CEO 2026-08-23'te şirket sayfası açtı. URL CEO'dan alınacak, `src/lib/site.ts`'e `linkedin` alanı + `Footer.tsx`'e link. (Metinler hazır: giriş gönderisi + Hakkında + slogan + 20 uzmanlık etiketi — bu oturumda yazıldı, dosyaya kaydedilmedi.)
2. `hello@corvus-tech.co` posta kutusu kur (GoDaddy MX) → `site.ts`'teki kişisel Gmail'i değiştir. LinkedIn'de kurumsal görünüm için gerekli.
3. v1.1: proje detaylarına gerçek ekran görüntüleri (Quill/marketing, TripWalkers/fastlane, Amelie.co/Logo)
4. TODO #9: logo mavisi `#5B8CFF` ile `--c-ios` çakışması — CEO kararı bekliyor
5. İsteğe bağlı: GitHub repo push (şu an sadece lokal git)

## Çalıştırma
- Dev: `npm run dev` (port 3000 doluysa 3001)
- Build: `npm run build`
- İçerik düzenleme: sadece `src/data/projects.ts` (yeni proje eklemek = array'e obje eklemek)
